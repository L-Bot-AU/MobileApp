<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:LBot.Views"
       x:Class="LBot.AppShell">


    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid BackgroundColor="White"
                  HeightRequest="200">
                <Image Aspect="AspectFit" Source="logo.png" Opacity="1" HorizontalOptions="Center"/>
            </Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    <FlyoutItem Title="Home" x:Name="Home0">
        <ShellContent ContentTemplate="{DataTemplate views:Home}"/>
    </FlyoutItem>
    <FlyoutItem Title="Other Home" x:Name="Home1">
        <ShellContent ContentTemplate="{DataTemplate views:Home1}"/>
    </FlyoutItem>
    <FlyoutItem Title="Junior Library">
        <ShellContent ContentTemplate="{DataTemplate views:Jnr}"/>
    </FlyoutItem>
    <FlyoutItem Title="Senior Library">
        <ShellContent ContentTemplate="{DataTemplate views:Snr}"/>
    </FlyoutItem>
    <FlyoutItem Title="About">
        <ShellContent ContentTemplate="{DataTemplate views:About}"/>
    </FlyoutItem>
    
    

    
   
</Shell>
